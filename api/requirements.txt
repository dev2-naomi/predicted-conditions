# Lambda container dependencies — mirrors the root requirements.txt (agent.py,
# step_loader.py, and tools/ need the full dependency set, not just a slim
# API layer) plus the AWS/API-only additions.
langgraph>=0.2.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
typing_extensions>=4.9.0

# API / AWS additions (not needed by langgraph dev, only by the Lambda shim)
fastapi>=0.115.0
mangum>=0.19.0
uvicorn[standard]>=0.32.0
boto3>=1.34.0
langgraph-checkpoint-aws>=0.1.0
